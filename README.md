# Feoidle — Phaser + Vite starter

Petit starter pour un jeu incrémental medieval/fantastique avec Phaser.

Install & run:

```bash
npm install
npm run dev
```

Ouvrir http://localhost:5173/ (Vite default) si le navigateur ne s'ouvre pas.
